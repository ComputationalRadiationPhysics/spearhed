#pragma once

#include "llamaLite/llamaLite.hpp"

namespace pmacc::spearhed
{
    namespace tags
    {
        DEFINE_TAG(x);
        DEFINE_TAG(y);
        DEFINE_TAG(z);

    } // namespace tags

} // namespace pmacc::spearhed
